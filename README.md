# Inventory-balancer-project-python
A web-based Inventory Management System developed using Python and Streamlit. The application supports full CRUD operations (Create, Read, Update, Delete) with JSON-based data storage. It includes category management and an interactive UI built with Streamlit.
